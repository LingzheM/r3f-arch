# STATE —— 当前进度

> 每个 M 验收通过后更新本文件。新对话开场第一个读的就是它。

**最后更新**：2026-08-18
**当前对话**：#1（M1–M2）
**当前里程碑**：**M2 墙角严丝合缝**

---

## 进度

| M | 名称 | 状态 |
|---|---|---|
| M1 | 能画一堵墙 | ✅ 验收通过 |
| M2 | 墙角严丝合缝 | 🔨 进行中 |
| M3 | 选中与交互状态机 | ⬜ |
| M4 | 拖动、撤销、脏传播 | ⬜ |
| M5 | 节点注册表 | ⬜ |
| M6 | 楼板、天花、柱 | ⬜ |
| M7 | 门窗与开洞 | ⬜ |
| M8 | 多楼层 | ⬜ |
| M9 | 存档、导入导出、迁移 | ⬜ |
| M10 | 屋顶 | ⬜ |
| M11 | 楼梯 | ⬜ |
| M12 | UI 外壳 | ⬜ |
| M13 | 属性面板与大纲 | ⬜ |
| M14 | 材质与上色 | ⬜ |

---

## M2 的下一步

按 `m2-miter.html` 的十二步。建议顺序：

1. 第 02 + 10 步（`geometry-2d` 扩容 + 测试）—— 纯函数，先跑绿
2. 第 03–07 步（斜接算法 + 轮廓）
3. 第 08/11 步（`wall-mitering.test.ts`）—— **必须在接 Three 之前全绿**
4. 第 01 步（手搓 BufferGeometry，可选但推荐）
5. 第 09–11 步（Shape+Extrude、context、接线）
6. 第 12 步（miter limit）

---

## 当前的"故意保留的缺陷"

| 缺陷 | 引入于 | 到期 |
|---|---|---|
| 改一堵墙 → 全场景 miter 重算 + 所有墙重建几何 | M2 | M4 |
| 交点检测 O(交点×墙)，无空间网格 | M2 | M4 |
| 画墙端点不吸附，靠手抖对齐 | M2 | M4 |
| `WallTool` 用裸 `useState`，第二个工具会打架 | M1 | M3 |
| 墙高存在 `wall` 自己身上，无楼层概念 | M1 | M8 |
| 刷新即丢，无存档 | M1 | M9 |
| UV 是 ExtrudeGeometry 默认的，贴图跨墙接不上 | M2 | M14 |
| `src/core/schema/camera.ts` 未使用（照抄原项目时带入） | M1 | 待定 |

---

## 待处理（非里程碑）

- **类型检查是红的，且已推上 origin**——见 `deploy-and-ci.md`。
  开工 M3 之前应先修绿，否则后面每次 `pnpm verify` 都分不清是新错还是旧错。
- 尚无 CI。架构层边界的 lint 规则没有 CI 等于没有——它只在你本地跑 `pnpm lint` 时才生效。
  建议在 M3 之前补一个最小 Actions（`check-types` + `lint` + `test`）。
- `dist/` 与 `index.html` 的 git 状态见 `deploy-and-ci.md`。

## 已知的实际代码 vs 文档偏差

- `eslint.config.js` —— `allowTypeImports` 必须写在每条 `paths` / `patterns` 条目内部，
  不是顶层选项。文档已修正。用户还补了 `languageOptions.parser`（`.tsx` 解析需要）。
